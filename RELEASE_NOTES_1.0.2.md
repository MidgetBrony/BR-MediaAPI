# BR-MediaAPI 1.0.2

- Prevents custom source boxes from duplicating media already restored elsewhere in the room.
- Returns deleted custom media to its source box immediately.
- Adds BOXROOM-native source-box search for every registered media type.
- Search identifies spawned and loading items and can retrieve an existing placed case.
- Corrects the search-menu close lifecycle so Harmony initializes without an error.
